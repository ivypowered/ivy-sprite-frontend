import { Metadata } from "next";

export const metadata: Metadata = {
    title: "ivy-sprite | link",
    description: "Link your wallet to the ivy-sprite bot",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
